apiVersion: v1
kind: Service
metadata:
  name: front-end-api
  namespace: web
  labels:
    app: front-end-api
spec:
  # gcloud compute addresses describe mythica-gke-public-ip --region us-central1
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
  selector:
    app: front-end-api

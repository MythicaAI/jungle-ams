apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.api.webFront.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "api.commonLabels" . | nindent 4 }}
    {{- include "api.componentLabels" .Values.api.webFront | nindent 4 }}
  annotations:
    {{- if eq .Values.gke.enabled true }}
    networking.gke.io/load-balancer-type: "Internal"
    cloud.google.com/neg: '{"exposed_ports": {"80":{}}}'
    {{- end }}
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app: {{ .Values.api.webFront.name }}

apiVersion: v1
kind: Service
metadata:
  name: lets-encrypt
  namespace: api
  labels:
    app: lets-encrypt
    tier: web
    version: "1.0"
    release: production
  annotations:
    networking.gke.io/load-balancer-type: "Internal"
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app: lets-encrypt

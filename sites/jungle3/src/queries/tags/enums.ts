export enum TagsQuery {
  TAGS_LIST_ASSETS = "tagsListAssets",
  TAGS_LIST_FILES = "tagsListFiles",
  ASSETS_BY_TAG = "assetsByTag",
  TAGS_LIST_ALL = "tagsListAll",
}

export enum TagsApiPath {
  TAGS = "/tags",
  TYPES = "/types",
  FILTER = "/filter",
}

export enum TagType {
  ASSET = "asset",
  FILE = "file",
}
